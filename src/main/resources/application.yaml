spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/new_db
    username: alrhazes
    password: a2144
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # Auto-create/update database schema
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL 8 dialect
        show_sql: true # Show SQL queries in logs (for debugging)
        format_sql: true # Format SQL queries in logs (for readability)
        use-new-id-generator-mappings: false # Use legacy ID generator
